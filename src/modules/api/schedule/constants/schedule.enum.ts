export enum ScheduleTitleLengthEnum {
  MIN = 0,
  MAX = 50,
}

export enum ScheduleAreaStreetAddressLengthEnum {
  MIN = 1,
  MAX = 100,
}

export enum ScheduleAreaNameLengthEnum {
  MIN = 1,
  MAX = 30,
}

export enum ScheduleAreaLatitudeLengthEnum {
  MIN = 1,
  MAX = 100,
}

export enum ScheduleAreaLongitudeLengthEnum {
  MIN = 1,
  MAX = 100,
}

export enum ScheduleAreaTimeLengthEnum {
  MIN = 0,
  MAX = 10,
}
