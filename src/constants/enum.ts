export enum StatusColumnEnum {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
}

export enum CheckColumnEnum {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum StatusOrCheckColumnLengthEnum {
  CHECK = 10,
  STATUS = 10,
}
